import { MediaUrl } from "../user/user";

export class Banner {
    id: string;
    title: string;
    title_translations: object;
    meta: object;
    sort_order: string;
    mediaurls: MediaUrl;
}
